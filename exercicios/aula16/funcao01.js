function evenorodd(n) { // parâmetro
    if (n%2 == 0) {
        return 'Par' // retorno
    } else {
        return "Impar"
    }
}

/*

let firstResult = evenorodd(4)
console.log(firstResult) 
 
*/

console.log(evenorodd(423)) // chamada